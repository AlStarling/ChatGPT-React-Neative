import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  errorContainer: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    marginHorizontal: 20,
  },
  errorText: {
    fontSize: 25,
    color: '#ffffff',
    textAlign: 'center',

  },
  icon: {
  color: '#ffffff',
  fontSize: 200,
  marginBottom: 20,
  }
});